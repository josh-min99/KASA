# OPEN QUESTIONS

내가(드라이랩 담당) 판단할 수 없어 남겨두는 것들. 아침에 결정이 필요한 순서로 정렬.

---

## 1. 이전 분석 전제와의 충돌 — 판단 필요

**하려던 것**: 프롬프트에 주어진 전제("20개 규모 유전자 모듈에서 분해능이 없다")를
그대로 깔고 시작했다.

**어긋난 지점**: Stage 4에서 정의한 I4 지표(clock 유전자 |log2FC| 중앙값 ÷ 같은 데이터셋
전체 유전자 |log2FC| 중앙값)는 16개 유전자 모듈만으로 우주비행(3.11)과 HLU(0.78)를
명확히 판별했다. 겹치지도 않는다(범위 1.90–5.28 vs 0.63–1.38).

**차이의 원인 추정**: 이전 분석은 모듈의 *군간 차이*를 검정했고(집단 비교),
이번 I4는 *같은 데이터셋 내부의 상대적 크기*를 본다(자기 정규화).
후자가 데이터셋 간 규모 차이에 훨씬 강건하다.

**결정해야 할 것**: 이전 결론("모듈 규모에서 분해능 없음")을 수정할 것인가,
아니면 "집단 비교 검정에서는 분해능이 없으나 자기 정규화 지표에서는 있다"로
조건을 붙여 양립시킬 것인가. 계획서 서술에 직접 영향을 준다.

---

## 2. 결론의 표현 수위 — 판단 필요

**상황**: 세 접근이 모두 같은 방향을 가리킨다.
- 다조직 매칭 (RR-1 8조직 vs HLU 4조직)
- 전역 정규화 (I4)
- 단일 스터디 앵커 (OSD-21, 배치 교란 없음)

**그러나 각각 약점이 있다**:
- HLU 세트가 4조직뿐이고, 그중 비장은 DEG 7개로 사실상 신호 없음
- 앵커는 조직 1종(비복근), 군당 n=4–5
- I4는 원논문에 없는, 이번에 새로 정의한 지표

**결정해야 할 것**: 계획서에 어느 수위로 쓸 것인가.
(a) "하중 변화만으로는 재현되지 않는다" — 단정
(b) "재현되지 않는 것으로 보이며, 확정에는 시간 해상도 데이터가 필요하다" — 완화

개인적으로는 (b)가 방어 가능하다고 보지만, 학부 계획서에서 (a)가 더 읽히는 측면이 있다.

---

## 3. Fig 3 (OSD-21 앵커) 포함 여부 — 판단 필요

논리적으로는 가장 강한 증거다(단일 스터디, 배치 교란 없음, 착륙 후 채취까지 통제).
그러나 조직 1종, 군당 n=4–5로 작다. 5페이지 제약에서
Fig 2만 본문에 넣고 Fig 3은 부록/발표자료로 돌릴지 판단이 필요하다.

---

## 4. 막힌 것 (참고용, 결정 불필요)

### 4-1. OSD-21에 GeneLab DE 테이블이 없음
구형 마이크로어레이 스터디라 DE 결과가 제공되지 않는다.
정규화 발현행렬 + 샘플 테이블에서 직접 군 평균차와 CI를 계산해 해결했다
(`src/s3d_osd21_anchor.py`). 다만 GeneLab의 DE 파이프라인과 동일하지 않으므로,
다른 스터디의 log2FC와 나란히 놓을 때는 주의가 필요하다.

### 4-2. 다조직 HLU 세트 확보에 두 번 실패
- OSD-334/335/336/337: 발현 데이터가 아예 없음(miRNA/대사체 계열)
- RR-6 계열(OSD-243~248): DEG 0–7개로 대부분 신호 없음
최종적으로 OSD-202/203/211/237(동일 HLU×방사선 2×2 설계)로 해결했다.

### 4-3. 정규식 오탐 필터가 정탐을 삭제한 사고 (2회)
- Stage 3c: 조사군 배제용 `irradiat` 패턴이 `non-irradiated`까지 매칭해
  필요한 대비를 전부 삭제. `(?<!non-)irradiated`로 수정.
- (이전 세션에서도 같은 유형이 있었음)
**교훈**: 배제 패턴을 추가할 때마다 그것이 무엇을 지우는지 역으로 확인해야 한다.

### 4-4. 원시 데이터 1.5GB를 git에 커밋했다가 되돌림
`data/raw/`를 `.gitignore`에 추가하고 재커밋했다. push 전에 발견해 원격에는 영향 없음.
재다운로드는 `src/s0_download.py`로 가능하다.

---

## 5. 확인했으나 이번엔 쓰지 않은 것

- **OSD-770 (가자미근, 비행)**: 희생 시각이 기록된 것으로 탐지됐다(`data_inventory.md`).
  이번 분석에는 쓰지 않았으나, 위상 관련 후속 분석에서 확인해볼 가치가 있다.
- 조직 매칭 표의 나머지 스터디(골수, 등쪽 피부 일부 등)는 다운로드는 마쳤으나
  단일실험 매칭 조건에 맞지 않아 이번 분석에서 제외했다. `data/raw/`에 남아 있다.
