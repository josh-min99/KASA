# PROGRESS

시간순 작업 로그. 각 Stage 종료 시 append.

---

[2026-07-29 01:20] Stage -1 (준비) | 명세 문서 확인 및 디렉토리 생성 | 결과: `docs/proposal_draft.md` 확인 완료, §3 이 드라이랩 명세. **`docs/prior_analysis.md` 는 존재하지 않음** — 프롬프트에 명시된 전제(희생 시각 미기록 / 착륙 후 채취 / SCN 없음 / 20개 모듈 분해능 없음)와 기존 `드라이랩_분석보고.md` 로 갈음함. `data/raw`, `data/processed`, `src`, `results/figures`, `results/tables` 생성 | 판정: 진행 가능 | 다음: Stage 0 데이터 확보

[2026-07-29 01:52] Stage 0 (진행중) | OSD-21 앵커 + §3.3 조직매칭 26건 다운로드 시작 (백그라운드) | 결과: OSD-21 포함 17건 확보 진행중 | 판정: **OSD-21 확보 성공 → Stage 0 완료조건 충족** | 다음: 완료 후 data_inventory.md 생성

[2026-07-29 02:05] Stage 1 | Life 2020 (PMC7555136) 전문에서 방법론 확보 후 재현. GLDS/OSD-98,99,101,102,103,104,105,168 의 GeneLab DE 테이블 8/8 확보. 대비 방향(GC v FLT)을 부호반전으로 보정 | 결과: A. Bmal1 8/8 조직 상향 / B. Per2 5/5 근육 하향 / C. Per2 부신 FDR 0.998·간 0.971 비유의, 신장 0.048 경계 | 판정: **재현 성공 (방향·크기 모두 원논문과 일치)** | 다음: Stage 2 양성대조

[2026-07-29 02:35] Stage 0 (완료) | 26건 시도 → ISA·발현행렬 전건 확보, 실패 0건. data/raw 1.5GB (gitignore) | 결과: data_inventory.md 생성 | 판정: **완료** | 다음: —

[2026-07-29 02:48] Stage 2 (초판) | 근위축 마커 Fbxo32/Trim63 로 양성대조 | 결과: 2/9 → 기준상 실패 | 판정: **판정 보류** — 대비 선택 오류 발견(OSD-876/880 은 'Unloaded **and Reloaded**', 935 는 x-ray+corticosterone 병용, 949 는 AMPKα 녹아웃 배경). 파이프라인 실패인지 대비 선택 실패인지 구별 불가 | 다음: 대비 선택 규칙화 후 재실행

[2026-07-29 03:02] Stage 2 (개정, s2b) | 대비를 '추가 요인 최소' 규칙으로 재선택 + 파이프라인 무결성 지표(DEG 개수) + 만성 섬유전환 마커(Myh) 추가 | 결과: 무결성 9/9 데이터셋에서 DEG 검출(중앙값 1,159개) / 급성 마커 1/9 / 만성 마커 Myh7하향 2/9·Myh4상향 2/9. OSD-935 soleus HLU 에서 Myh4 +4.40, Myh2 -3.80, Myh7 -1.08 (전부 FDR=0.000) 로 교과서적 느린→빠른 섬유 전환 확인 | 판정: **조건부 통과** — 파이프라인은 정상이나 생물학적 신호 강도가 데이터셋마다 크게 다름. 급성 마커가 안 잡히는 것은 37일 만성기라는 시점 때문으로 해석됨(비행 soleus 에서는 오히려 유의하게 하향) | 다음: Stage 3 은 데이터셋별 양성대조 통과 여부로 게이트를 걸어 진행

[2026-07-29 04:10] Stage 3 (초판 s3) | Stage1 지표를 HLU 7건에 적용 | 결과: Bmal1 부호일치 FLIGHT 1.00 vs HLU 0.571, 프로파일 r 중앙값 0.857 vs 0.000 | 판정: **결과로 채택 불가** — FLIGHT 8조직은 RR-1 단일미션이고 HLU 7건은 서로 다른 7개 실험이라 배치 교란. 차이의 상당부분이 '같은 미션 vs 다른 실험'일 수 있음 | 다음: 양쪽을 단일실험 다조직으로 맞춤

[2026-07-29 04:25] Stage 3b | 매칭 시도: RR-6(6조직) + OSD-334~337(4조직) | 결과: OSD-334~337 은 발현 데이터가 아예 없음(miRNA/대사체). RR-6 는 DEG 0~7개로 대부분 신호 없음(thymus만 5031) | 판정: **RR-6 는 '재현 실패'가 아니라 '신호 없음' — 재현 검정에 사용 불가** | 다음: 다른 매칭 세트 탐색

[2026-07-29 04:45] Stage 3c | OSD-202(뇌)/203(망막)/211(비장)/237(피부) 가 동일한 HLU x 방사선 2x2 설계임을 발견. '비조사 HLU vs 비조사 정상하중' 대비로 하중만 분리 | 버그: 조사군 배제용 정규식 `irradiat` 가 `non-irradiated` 까지 매칭해 필요한 대비를 전부 삭제. `(?<!non-)irradiated` 로 수정 | 결과: FLIGHT-A(8조직) Bmal1 +1.105·부호일치 1.00·프로파일 r 0.856 vs HLU-A(4조직) Bmal1 +0.098·부호일치 0.50·프로파일 r -0.199 | 판정: **배치 교란 제거 후에도 HLU 는 비행 패턴을 재현하지 않음** | 다음: Stage 4 정규화·검정력

[2026-07-29 05:00] Stage 4 | (1) clock |log2FC| 를 같은 데이터셋의 전역 |log2FC| 로 정규화 (2) RR-1 수준 효과의 검출 가능성 | 결과: clock enrichment FLIGHT-A 3.114(1.90~5.28) vs HLU-A 0.778(0.63~1.38). clock 백분위 0.808 vs 0.428. Mann-Whitney p=0.004 (n=8 vs 4, 참고치). 검정력: HLU 데이터셋 전역 log2FC SD 0.93~1.14 라 RR-1 수준 효과(1.105)는 z=1.13 으로 묻힘 | 판정: **모듈 수준 = 판정 가능(HLU 는 clock 특이적 반응 없음) / 단일 유전자 수준 = 판정 불가** | 다음: OSD-21 앵커

[2026-07-29 05:10] Stage 3d | OSD-21 앵커. DE 테이블이 없어 정규화 발현행렬+샘플표에서 직접 군간 차이 계산. 5군 전부 매칭(Flight 4/GC 4/HLU 5/HLU+Reloaded 5/NormalLoaded 5) | 결과: Flight vs GC 에서 Arntl +0.458(d=2.49), Per1 +1.585(d=3.21), Nr1d1 +0.374(d=3.13), |차이|>0.5 3/15. HLU vs NormalLoaded 는 |차이|>0.5 1/15 이고 Arntl 은 -0.302 로 반대방향. HLU+Reloaded vs HLU 는 |차이|>0.5 1/15 | 판정: **단일 스터디 내부에서 Stage 3c/4 결론을 독립 재현. 배치 교란 없음** | 다음: Stage 5

[2026-07-29 05:45] Stage 5 | 계획서용 그림 3장 + 문서 2편 작성. Fig3 캡션의 U+2212 가 Malgun Gothic 에 없어 □ 로 깨진 것과 x축 라벨 겹침을 수정 | 결과: results/figures/fig1~3, docs/dry_lab_section.md, docs/expected_results.md | 판정: **완료** | 다음: FINAL_REPORT, OPEN_QUESTIONS

[2026-07-29 06:00] 종료 | FINAL_REPORT.md, OPEN_QUESTIONS.md 작성 | 결과: Stage 0·1·5 완료, Stage 2 조건부 통과, Stage 3 완료, Stage 4 부분(모듈 수준만 판정 가능) | 판정: **야간 작업 종료** | 다음: 아침에 사용자 판단 3건 (결론 수위 / 이전 전제와의 충돌 / Fig3 포함 여부)

---
## v2 — 조직 매칭 재분석 (2026-07-29 후속)

[2026-07-29 10:20] v2 준비 | 어제 문서 4종 정독. FLIGHT-A(EDL·부신·비복근·신장·간·사두근·가자미근·전경골근)와 HLU-A(뇌·망막·비장·등쪽피부)의 조직이 하나도 겹치지 않음을 확인 | 판정: **조직 효과와 처리 효과가 완전 교락. 어제 '3.11 vs 0.78' 은 이 상태로는 해석 불가** | 다음: 조직 매칭 재분석

[2026-07-29 10:35] v2 대비 선택 규칙 재설계 | 어제 정규식 오탐 사고(irradiat 가 non-irradiated 삭제)가 두 번 있었으므로 블랙리스트 방식을 버림. 대신 '중력 이외의 모든 배경 성분이 양쪽에서 동일할 것' 을 요구하는 규칙으로 전환. 대비 문자열을 ' & ' 로 분해해 중력 성분/배경 성분으로 나눈 뒤 배경 다중집합 일치를 검사 | 결과: 유전형(Zfp697 mKO, AMPKalpha mKO)·병용처치(x-ray, corticosterone)·주사(tetanus, CpG)·시점·성별·계통이 자동 매칭됨 | 판정: 규칙 채택

[2026-07-29 10:45] v2 규칙 사전 검증 | 13개 케이스로 규칙이 무엇을 채택/탈락시키는지 역방향 확인 | 결과: **12/13 일치, 1건 오류 발견** — OSD-758 의 `Space Flight & 1G by centrifugation` 이 채택됨. 궤도에서 원심분리로 1G 를 복원한 군이라 중력 감소가 아님. OSD-288·714·238 도 같은 구조 | 조치: RESTORED_G 규칙 추가 후 재검증 13/13 통과 | 판정: **실행 전에 잡음. 어제와 같은 유형의 사고를 사전 검증으로 차단** | 다음: 본 분석 실행

[2026-07-29 12:10] v2 본 분석 | 5개 조직(비복근·가자미근·비장·망막·등쪽피부)에서 24개 데이터셋 대비 선택 후 I4 산출. 품질 게이트 DEG>=30 | 결과: 채택 15/24. 제외 사유는 신호 없음 8건, 대비 불가 1건(OSD-935 x-ray+corticosterone 병용) | 판정: **조직 내 비교에서 방향이 갈림** — 비복근·가자미근은 비행>HLU, 망막·등쪽피부는 HLU>비행, 비장은 HLU 양쪽 신호 없어 비교 불가 | 다음: 미션 효과 확인

[2026-07-29 12:30] v2 미션 확인 | 비행 데이터셋 I4 가 크게 갈리는 것을 보고 RR-1 소속 여부로 분리 | 결과: **RR-1 8건 중앙값 3.114(1.90~5.28) vs 기타 미션 8건 중앙값 0.750(0.53~1.53) vs HLU 5건 1.069(0.75~1.38)**. 어제 FLIGHT-A 8개는 전부 RR-1 이었음. 기타 미션 비행은 HLU 보다 오히려 낮다 | 판정: **어제 '비행 3.11' 은 우주비행 일반이 아니라 RR-1 특이값. 조직 교락과 미션 교락이 같은 방향으로 겹쳐 있었음** | 다음: 판정 및 보고서

[2026-07-29 13:00] v2 판정 | 어제 결론 처리 결정 | 결과: **철회 3건** — 'clock/전역 비 3.11 vs 0.78', '하중 변화만으로는 재현되지 않는다', '조직쌍 프로파일 상관 0.856 vs -0.199'. **유지 4건** — Life 2020 재현, 파이프라인 무결성, 단일 유전자 판정 불가, 위상 정보 부재로 인한 웻랩 필요성. **조건부 유지 1건** — OSD-21 앵커는 조직 매칭·배치 교란이 모두 없으므로 '근육에서' 로 한정해 유지 | 판정: **어제 결론을 방어하지 않고 철회. 다만 웻랩 정당화 논지는 오히려 강화됨(조직·미션 변동이 처리 효과보다 큼)** | 다음: 그림·보고서 완료

[2026-07-29 13:15] v2 산출물 | forest plot + REPORT_V2.md | 결과: results/v2/figures/fig2_v2_tissue_matched.png (조직 이름이 데이터셋 라벨과 겹치고 캡션이 x축을 덮던 것 수정), results/v2/REPORT_V2.md | 판정: **완료** | 다음: 커밋

[2026-07-30 09:40] v2 문서화 | 계획서 초안(§2 나 드라이랩 / §3 예상되는 결과)을 v2 수치로 보강 + 작업기록 문서 작성. 둘 다 구글 독스 붙여넣기용 HTML | 갱신 내용: (a) 데이터 절의 '6개 조직'을 '5개 조직 + 골수는 대비 불가로 제외'로 수정 (b) [수정 예정] 표시 해소 — 조직별 표(표 3)와 미션별 표(표 4) 신설 (c) 대비 선택 규칙·품질 게이트를 방법에 명시 (d) 보정·검정력은 '예정'으로 표기 유지(미실시) (e) §3(2)를 세 경우 중 '판정 불가'가 실제로 나왔음을 서술하도록 재작성 (f) 앵커 결론을 '근육에 한정'으로 조건화 (g) 그림 1을 v2 forest plot 으로 교체 | 판정: **완료** | 다음: 커밋

[2026-07-31 10:15] 그림 2 부호화 누락 수정 | 기존 fig3_osd21_anchor.png 은 마커 채움으로 비행 arm(지상대조·우주비행)과 HLU arm(정상하중·HLU·HLU+재하중)을 구분했으나 범례도 캡션 설명도 없었음. 캡션이 'arm 내부 대비만 사용' 이라고 서술하면서 정작 어느 점이 어느 arm 인지 표시하지 않아 그림을 올바르게 읽을 수 없는 상태였음 | 조치: 범례 추가(채운 점/빈 점/군 평균), arm 경계 세로 점선 추가, 캡션에 부호화 명시. 패널 내 arm 라벨은 유전자 제목과 겹쳐 제거하고 범례로 대체 | 결과: results/v2/figures/fig3_v2_osd21_anchor.png (기존 v1 파일은 보존). 계획서 문서의 그림 2 캡션도 갱신 | 판정: **완료**

[2026-07-31 11:05] 그림 2 재수정 — 중복 부호화 제거 | 앞선 수정(범례 추가)은 방향이 틀렸다는 지적을 받음. x축에 이미 군 이름이 표기돼 있으므로 마커 채움은 같은 정보의 중복 부호화였고, 그것을 설명하려고 범례를 더한 것은 요소만 늘린 셈 | 조치: 마커를 통일하고 범례 제거. arm 구분은 세로 점선 하나로만 표시하고 캡션에서 '점선 왼쪽=비행 실험, 오른쪽=지상 실험' 으로 서술 | 결과: 그림 요소가 줄고 읽기 쉬워짐. 계획서 캡션도 동일하게 수정 | 판정: **완료**

[2026-07-31 13:40] 표 3·4 단위 표기 누락 수정 | 두 표의 숫자가 clock 특이 반응 지수임이 캡션에만 있고 열 제목에는 없어 무엇의 값인지 알 수 없다는 지적 | 조치: 두 표에 'clock 특이 반응 지수 (1.0 = 전체 유전자와 동일)' 스팬 헤더 추가. 캡션에 지수가 비율이라 단위가 없다는 점과 계산식을 명시. 표 4 의 'n' 을 '데이터셋 수' 로 풀어 씀 | 판정: **완료**

[2026-07-31 15:20] 그림 2 산점 배치 수정 | 지적: x축이 범주형인데 점이 가로로 흩어져 있어 가로 위치에 의미가 있는 것처럼 보임 | 원인 확인: np.linspace 로 모든 점을 샘플 순서대로 좌->우 배치하고 있었음. 무작위 jitter 도 아니어서 순서가 의미를 갖는 것처럼 읽힘. 또 겹치지 않는 군까지 벌리고 있었음 | 사전 확인: 군 내 y값 최소 간격을 계산한 결과 20개 조합 중 10개가 축 높이의 2% 이내로 붙어 있어(최소 0.1%) 완전히 겹치지 않게 찍는 것은 불가능 | 조치: 겹치는 점만 중앙 기준 좌우 대칭으로 벌리는 beeswarm 방식으로 교체. 겹치지 않는 점은 정확히 중앙. 캡션에 '가로 위치에는 의미가 없다' 명시 | 판정: **완료**

[2026-07-31 16:40] 앵커(그림 2) 재검토 — 유전자 선택 편향 발견, 역할 재정의
  발단: "그림 2가 계획서에서 어떤 역할을 하는가" 를 점검.
  발견 1 (순환 논증): 15개 clock 유전자 중 4개만 그렸는데, 그 4개가 전부
    '비행에서 CI가 0을 배제 + 상향' 인 유전자였다. 유의한 5개 중 유일하게 하향인
    Hlf(-0.215, d=-2.63)만 제외돼 있었다. 사전 지정 유전자 Per2 는 +0.037(d=0.33)로
    재현 안 되는데 그림에 없었다.
  발견 2 (기준선 artifact): 절대 발현량을 그려서 두 arm 기준선 차이(Arntl 0.49)가
    주장 효과(0.46)보다 컸다. 캡션이 무시하라고 한 간격이 가장 눈에 띄었다.
  발견 3 (핵심): 15개 전부 × 4개 대비로 재계산하니 |차이| 중앙값은 0.138~0.224 로
    비슷한데 CI가 0을 배제하는 개수는 8/5/0/0 이고, 그 순서가 CI 폭
    (0.43/0.64/0.82/0.95)의 역순과 정확히 일치했다. 즉 n=4~5 에서 '유의한 개수'는
    처리가 아니라 군내 분산이 정한다. 비행 5개 vs HLU 0개도 효과크기 차이가 아님
    (0.215 대 0.182).
  기각한 가설: 8/15 가 3.5시간 희생 시각 차이 때문이라고 추측했으나, 시각 차이만
    있는 대비(HLU+재하중 vs HLU)는 0/15, |d| 중앙값 0.39 로 최소. 시간이 아니라 분산.
    (OSD-21 프로토콜 원문에서 3.5h 설계 확인: "unsuspended for 3.5 h ... to match the
     time between landing and death for the STS-108 mice")
  조치: 그림 2 전면 재작성 (A 15개 전부 forest + B 사전지정 유전자 개체값 +
    C 효과크기 대 정밀도). 계획서에 §3(2) 앵커 절 신설, 표 5 추가, 뒤 절 재번호.
    '근육 한정 신호 존재 가능' -> '미확정' 으로 하향.
  역할 재정의: 앵커는 '철회 후 남은 양성 근거'가 아니라 '교란을 다 제거한 최선의
    설계에서도 판정이 불가능함을 보이는 사례'. 웻랩 정당화가 강해졌다.
  판정: **완료**

[2026-07-31 18:05] 표 3 재검증 + 표 4 조직 매칭 (사용자 지시 2건)
  [작업 1] 저장소 CSV 오염 원인 규명과 복구
    발견: v2_per_dataset.csv 가 보고서 §2 와 불일치 (채택 13 vs 15). 오염본은
      OSD-949 를 AMPKalpha mKO, OSD-211/203/237 을 cobalt-57 조사군,
      OSD-758 을 0.33G 원심분리군으로 선택. 전부 tie-break 가 배제해야 할 것들.
    원인 조사: 현재 스크립트 + 현재 DE 파일로 find_contrast() 를 직접 호출한 결과
      5건 모두 깨끗한 대비를 정확히 선택. 규칙 자체는 정상.
      15:57 실행 시점의 DE 파일 상태가 달랐던 것으로 판단 (같은 실행에서
      OSD-254 다운로드 실패 기록이 있음). de_path() 가 '가장 작은 로컬 파일'을
      고르므로 다운로드 진행 중 실행하면 결과가 달라진다.
    복구: 오염본을 _stale_15h57_*.bak 로 보존 후 전체 재실행.
      -> 채택 15/24, 채택분 DEG 전부 일치, I4 전부 일치. **표 3 검증 완료.**
    미조치 위험: de_path() 파일 고정 필요. OPEN_QUESTIONS 성격으로 §9.6 에 기록.

  [작업 2] 표 4 조직 매칭
    발견: 표 4 는 RR-1(근육5+간·부신·신장) 대 기타 미션(가자미근2+비장·망막·피부6)
      비교였다. 겹치는 조직이 가자미근 하나뿐. v1 결론을 철회할 때 지적한 것과
      동일한 교락 구조를 표 4 에서 반복했다.
    미션 정체 확인 (ISA/프로토콜 원문): 기타 미션 = RR-23(770,506), RR-9(255),
      RR-7(254), JAXA MHU-1(288)/MHU-2(238)/MHU-1·4·5(714)/MHU-8(758).
      단일 미션이 아니라 잔여 범주였다.
    RR-1 8건을 v2 규칙으로 재계산 -> v1 값과 동일 (중앙값 3.114). 파이프라인
      버전 차이는 교락이 아니었음을 확인.
    층화 결과: 조직 미통제 4.15배 -> 근육 층화 2.51배 -> 가자미근(유일 매칭)
      1건 대 2건, 범위 거의 접함(1.899 대 0.766~1.529).
    조치: 표 4 를 층화 표로 교체. 결론을 '방향'(RR-1이 높다)에서 '폭'(데이터셋
      간 변동이 처리 간 차이보다 크다)으로 이동. 폭에 관한 주장은 교락과
      무관하게 성립한다.
    철회: "RR-1 3.11 대 기타 0.75 는 미션 효과"
    하향: "비행의 clock 반응은 특정 미션에 의존한다"
    유지: 표 3 전체, RR-1 개별 I4 값, "데이터셋 간 변동 > 처리 간 차이"
  판정: **완료**

[2026-07-31 19:30] 외부 피드백 반영 — 표 5 논증 결함 수정 외
  받은 지적을 전부 데이터로 검증한 뒤 적용했다. 지적은 모두 사실이었다.

  [1] 표 5 논증 결함 (가장 중요)
    지적: "유의 개수가 CI 폭의 역순과 일치한다"는 논증은 효과크기 순서도
      똑같이 일치하므로 두 설명을 구별하지 못한다.
    검증: 순위상관 효과크기 +0.949 / CI폭 -0.949. 대비 4개로는 구별 불가. 지적이 맞다.
    수정: 순위 논증을 삭제하고 크기 논증으로 교체.
      비행 0.215 vs HLU 0.182 = 차이 0.033. 이는 두 대비 CI 폭(0.64, 0.82)의
      5.2% / 4.0%. 효과크기 차이가 해상도 아래인데 유의 개수는 5 대 0으로 갈린다.
      -> 개수 차이가 효과크기로 설명 불가. 반박 가능한 순위 주장 제거.

  [2] 안 쓰던 카드 두 개 투입
    (a) 8/15 가 지상 대비(HLU+재하중 vs 정상하중)에서 나왔다. 이 스터디에서
        clock 신호가 가장 많이 잡힌 것이 우주비행이 아니라 지상 실험이다.
        '비행이 clock 을 특이적으로 건드린다'는 전제를 앵커 내부에서 반증한다.
    (b) 3.5시간 구간의 변화 0.138 = 비행 대비(0.215)의 64%.
        (5) 한계의 '위상 정보 부재'를 서술에서 실측으로 바꿨다.
        단 표 5 4행 라벨이 틀렸었다: "처리 동일, 희생 시각만 3.5시간 차이" ->
        재하중은 시간이 아니라 처치다. "시간 경과와 하중 재부하가 함께 변함"으로 수정.
        논증도 "시각 차이만으로" -> "3.5시간 구간으로" 완화.

  [3] 1단계 재현 판정 한정
    검증: Per2 는 비행 +0.037(d=0.33) 재현 안 됨, HLU 에서는 +0.267(d=1.39)로
      선행연구와 반대 방향. 1단계 근육 5건은 전부 RR-1.
    수정: "재현 성공" -> "RR-1 데이터 내부에서 성공". 재현조차 미션 의존적이라는
      사실을 판정 불가 논지에 편입.

  [4] 표 3 방향 반전의 미션 교락 명시
    검증: 비행 10건 중 RR-1 은 OSD-101(비복근), OSD-104(가자미근) 딱 2건.
      비행이 높은 두 조직에만 있고 방향이 뒤집힌 망막·등쪽피부에는 0건.
    수정: "조직을 맞춰도 방향이 갈리며, 그 갈림이 조직 때문인지 미션 때문인지조차
      분리되지 않는다"로 교체.

  [5] 생명과학 검증 필요 2건 -> OPEN_QUESTIONS.md 로 분리 (계획서에 넣지 않음)
    Q1 출력 유전자(Dbp/Tef/Hlf) 전부 음, 활성화 축 전부 양 -> 위상 이동 패턴인가
    Q2 Per1 +1.585 가 글루코코르티코이드 급성 반응인가 (재하중 군의 통제 성립 여부)
    둘 다 관측은 데이터로 확인했으나 해석이 배경지식 의존이므로 원 논문 확인 전 미사용.

  [6] 잔손질
    - 그림 2(C) 행 순서를 표 5 와 일치 (8,5,0,0)
    - 0을 배제한 5개 유전자 이름을 캡션·본문에 명시 (Arntl, Cry2, Nr1d1, Per1, Hlf)
    - 다중검정 미보정 명시 (15 x 0.05 = 0.75개/대비 기대). 개수는 기술 통계로 한정
    - (A) 3대비 / (C) 4대비인 이유를 캡션에 추가
    - 그림 1 캡션 OSD-935 줄 잘림 수정 (textwrap 으로 줄바꿈)
    - 용어 통일: 축 라벨 'I4' -> 'clock 특이 반응 지수'
    - '검정력(예정)' -> 앵커 분석이 실측으로 답했으므로 관계를 명시하고 예정 딱지 제거
    - 유니코드 마이너스가 폰트에서 두부로 깨져 ASCII 하이픈으로 교체 (재발)

  미적용 (사용자 판단 필요)
    - 표 2(4단계 절차)를 문장으로 풀어 분량 축소: 레이아웃 결정 사항
    - 표 3/그림 1 중 하나만 남기기: 이전 턴에서 제기, 미결
    - permutation 보정 실행 여부: 표 3·4용 절차로 남겨둠
  판정: **완료**

[2026-07-31 20:15] 3.5시간 논증 철회 — 잡음 바닥 미확인으로 과장했다
  발단: "현수가 HLU인가" 라는 질문에 답하려고 해당 문단을 다시 읽다가
    직전 턴에 내가 넣은 논증이 틀렸음을 발견했다.

  내가 쓴 것: "3.5시간 구간만으로 발생한 clock 변화 0.138 = 비행(0.215)의 64%"
  문제: 0.138 이 실제 효과인지 확인하지 않고 비율만 계산했다.
    해당 대비는 0/15 이고 CI 폭이 0.95 로 넷 중 가장 넓다.

  검증: 효과가 0 이어도 |차이| 중앙값은 0 이 아니다. 표본 잡음의 기대치가 있다.
    CI 폭 = 2*t*SE 에서 SE 를 역산하고, 반정규분포 중앙값 0.6745*SE 를 잡음 바닥으로 삼았다.

    대비                    관측    잡음바닥   배수    0배제
    HLU+재하중 vs 정상하중   0.224   0.063   3.53x   8/15
    우주비행 vs 지상대조      0.215   0.088   2.45x   5/15
    HLU vs 정상하중         0.182   0.120   1.51x   0/15
    HLU+재하중 vs HLU      0.138   0.139   0.99x   0/15

    -> 3.5시간 대비는 0.99배. 잡음 기대치와 사실상 같다.
       "3.5시간이 비행 효과의 64%를 만든다"는 근거가 없다. **철회.**

  대체 서술: 원저자는 착륙 후 채취 교란을 통제하려고 재하중군을 설계했는데,
    n=5 에서는 그 통제가 작동하는지 확인조차 불가능하다.
    통제군이 설계에 있어도 표본 수가 없으면 소용없다는 것이 오히려 정확한 논지다.

  부수 효과: 잡음 바닥 계산이 표 5 논증 자체를 개선했다.
    "효과크기는 비슷한데 개수가 갈린다"에서
    "잡음 대비로는 2.45 vs 1.51 로 완만한데 개수는 5 vs 0 으로 갈린다"로 정량화.
    표 5 에 '잡음 기대치 대비' 열 추가.

  산출물: src/v2_noise_floor.py, results/v2/noise_floor.txt
  판정: **완료 (직전 턴 결과물 일부 철회)**

[2026-08-02] 진행상황 종합 문서 작성
  docs/진행상황_종합.html — 구글 독스 붙여넣기용, 9개 절.
  기존 문서들이 시점별로 흩어져 있어(팀공유_진행상황, 작업기록_v2, REPORT_V2)
  전체를 한 번에 볼 수 있는 문서가 없었다.

  구성
    1 한눈에 보기        현재 상태 요약표
    2 무엇을 물었는가     연구 질문 + HLU 용어 설명 박스
    3 어떻게 했는가       단계 게이트 / 대비 선택 규칙 / 핵심 지표
    4 통계를 읽는 법      군간 차이, 신뢰구간(=CI), 0 배제, 잡음 기대치
                        Arntl 실제 값으로 워크드 예제. 생명과학 팀원용
    5 결과              1~4단계 + 미션 층화 + 앵커
    6 철회한 결론        6건 전체 + 방법상 문제 4건
    7 유지되는 결론      5건 + 논지 이동 도식
    8 남은 과제          담당자별
    9 산출물            경로별

  4절을 넣은 이유: 표 5의 '신뢰구간 폭'과 'CI가 0을 배제'가 같은 것(CI=신뢰구간)인데
    다른 이름으로 적혀 있어 혼동을 유발했다. 계획서 표 5 표기도 '신뢰구간'으로 통일했다.

  검증: 문서 수치 30건을 산출물 CSV와 대조. 전부 일치.
    (scratchpad/verify_doc.py — 채택 건수, 층화 중앙값, 표 3 HLU 값,
     앵커 4대비 |차이|와 0배제 개수, 본문 토큰 존재 여부)
  판정: **완료**

[2026-08-02] git push 실패 해결 — SSH 전환 완료
  증상: git add/commit/log 는 정상인데 push 만 실패. 재부팅으로만 복구.

  원인 규명 (GIT_TRACE 로 확인)
    git 은 자격증명 헬퍼를 명령 문자열 하나로 만들어 셸에 넘긴다.
      run_command:   'git credential-manager get'
      start_command: 'C:/Program Files/Git/usr/bin/sh.exe' -c 'git credential-manager get'
    문자열에 공백이 있어 항상 sh.exe 를 거친다. 어떤 헬퍼를 써도 '<헬퍼> get'
    형태라 공백을 피할 수 없다. 그래서 MSYS2 가 불안정해지면 HTTPS push 만 실패한다.

  배제한 원인
    msys-2.0.dll 중복(1개뿐) / Mandatory ASLR(NOTSET) /
    fstab 마운트 과다(2항목) / 환경변수 블록 과대(3.5KB)

  미규명
    MSYS2 가 왜 간헐적으로 죽는지는 확정 못 함. 좀비 프로세스 가설이 있으나
    (핸들 0·스레드 1 인 bash 발견) 그때도 sh.exe 는 정상이어서 인과 확정 불가.
    Defender 제외 목록은 관리자 권한이 필요해 미확인.
    -> 근본 원인 대신 '경로에서 제거' 로 우회했다.

  해결
    SSH 로 전환. 공백 없는 단일 경로를 core.sshCommand 로 주면 git 이 sh.exe 를
    거치지 않고 ssh.exe 를 직접 실행한다.
      git config --global core.sshCommand "C:/Windows/System32/OpenSSH/ssh.exe"
      git remote set-url origin git@github.com:josh-min99/KARA.git
    사용자가 id_ed25519.pub 를 GitHub 에 등록.

  검증
    ssh -T git@github.com -> "Hi josh-min99! You've successfully authenticated"
    git push --dry-run 추적 -> start_command: C:/Windows/System32/OpenSSH/ssh.exe ...
    진짜 sh.exe 호출 0 회 (ssh.exe 는 제외하고 셈)

  부수 작업
    tools/fix-git-push.ps1 — 다른 HTTPS 저장소용 진단/복구 스크립트.
    PowerShell 5.1 이 ps1 을 BOM 없으면 ANSI 로 읽어 한글이 깨지므로 UTF-8 BOM 으로 저장.
  판정: **완료 (근본 원인은 미규명, 경로 우회로 해결)**

---
## v3 — 리듬 데이터셋 감사 → 분석 → 계획서 개정 (2026-08-02)

[2026-08-02 R0] 채택 규칙 확정 + 역방향 검증
  기준 C1-C7 과 판정 순서를 본 검색 **이전에** 고정하고, 판정을 이미 아는 사례로
  역방향 적용해 규칙이 무엇을 채택/탈락시키는지 먼저 확인했다(정규식 오탐 사고 2회의 교훈).
  검증 중 규칙을 두 번 고쳤고, 고칠 때마다 재검증했다.
    - C3 을 C3a(>=24h)/C3b(>=7일)로 분리. 하나로 두면 60일 HDBR 이 탈락하는데,
      그 연구는 36시간 기록으로 acrophase 를 뽑아 위상 지연을 보고했다.
      파라미터 1회 추정에 필요한 길이와 추이 추정에 필요한 길이는 다르다.
    - LITERATURE / LEVEL_ONLY 계층 추가. C6 실패를 전부 REJECT 로 보내면 표가 비고,
      24h 커버리지가 없어도 '활동량 수준' 축은 볼 수 있는 자료(OSD-595)가 있다.
  최종 6/6 통과. 판정: **통과, R1 진행**

[2026-08-02 R1] 전수 탐색
  **초판 오류와 교정**: 검색 API 페이지네이션으로 스터디를 열거했더니 411건만 잡혔는데
  같은 저장소의 catalog_studies.csv 에는 626건이 있었다(222건 누락).
  build_catalog.py 처럼 ID 전수 순회로 교정. 또 중력 단서로 미리 거른 뒤 파일을 여는
  방식도 폐기하고 **전 스터디의 파일 목록을 열었다**(OSD-952 처럼 메타데이터에는
  transcription profiling 만 있고 부속 파일에 텔레메트리가 있는 구조를 놓치지 않기 위해).
  결과: 스터디 633건 / 파일 157,801개 → 리듬 후보 52개 파일 / 5개 스터디.
        그중 24시간 커버리지를 갖춘 것 0건. OSD-691 은 오탐(ATPase 활성).
  LSDA/NLSP 2,593건도 확인. STS-90 146건 전건 열람.
  판정: **G1 = 원자료 정량 사용 가능 2건 (3건 미만) → 계획된 축소 분기로 진행**

  주목할 부정 결과 3건 (계획서 §1 논거로 이관)
    1. 일주기 목적의 유일한 비행 실험 Neurolab STS-90 은 LSDA 에
       'Availability: Available offline / No data submitted by PI' — 원자료 부재
    2. 지상에 중력을 되돌려 넣은 유일한 통제 실험 AGBRESA 는 일주기 위상을 재지 않음
    3. OSDR 157,801 파일 중 24h 커버리지 0건

[2026-08-02 R2] 확보 및 공통 스키마화
  Helissen(PASS) / HDBR(CONDITIONAL) / OSD-595(LEVEL_ONLY) 원자료 확보, SHA256 고정.
  공통 long 스키마 16,561행. 군 라벨을 날짜에서 유도한 뒤 **데이터로 검증**:
  활동량 baseline→HLU 가 -82.9% / -78.4% 로 논문의 -85% 와 일치.
  판정: **완료**

[2026-08-02 R3+R3.5] 리듬 파라미터 + 잡음 바닥
  게이트 G2 (Helissen 재현) **10/10 통과**.
    활동 MESOR 0.272→0.046 (논문 0.26→0.04), 리듬검출 p 0.088→0.412 (논문 0.005→0.420)
    피하온도 MESOR 33.871→32.625 (논문 33.81→32.69), 진폭 0.856→0.176 (논문 0.78→0.23)
  잡음 바닥을 비율 논증보다 **먼저** 산출(3.5시간 논증 철회 사고의 교훈).
  판정: **통과**

[2026-08-02 R4] 축별 정합성
  활동량 수준: 지상 HLU -80.0/-79.3/-49.4% (3건 모두 0 배제)
               궤도 중력만 분리(초파리) +2.7% [-0.2, +6.0]
    -> 지상의 큰 감소를 중력 효과로 보기 어렵다
  체온 acrophase: 점추정 5/5 지연, 구간이 0 배제 3/5. 비행 영장류 보고 방향과 일치
  교락(종/측정방식/조명/기간)이 남은 비교는 방향만 주장하도록 규칙 고정.
  판정: **완료**

[2026-08-02 R5] 웻랩 검정력 — **초기 가설 일부 반증**
  내가 이 세션 초반에 사용자에게 한 진술: "HLU 중 활동량 리듬이 검출되지 않으므로
  1차 종말점을 심부체온으로 바꿔야 한다."
  검정력 분석 결과 **과한 진술이었다**. 개체 하나의 구간에서 진폭이 0과 구별되지 않는 것과,
  여러 개체를 모아 위상 이동을 검출하는 것은 다른 문제다.
  활동량으로도 dphi=2h 는 6~8마리로 검출된다. 폐기하지 않고 2차 종말점으로 유지한다.

  대신 실제로 중요한 것이 드러났다: **마스킹은 모든 지표에 해당한다.**
  HLU 중 위상 추정 SD 가 baseline 0.14~1.11h 에서 2.77~5.22h 로 3.6~38배 커진다.
  -> 가설 1(HLU 중 새 위상 형성)은 어떤 지표로도 판정이 어렵고,
     free-run 구간(가설 2)이 주 판정 경로가 되어야 한다.
  필요 마리수(baseline 3일, 처치 14일, 검정력 0.8):
     심부체온 dphi=1h 8 / 2h 4 / 3h 4   (HLU중 저하 9.0배)
     피하온도 12~16 / 6 / 4              (15.6~15.9배)
     활동량   >16 / 6~8 / 4~6            (3.6~38배)
  판정: **완료. 1차 심부체온 / 2차 활동량 권고**

[2026-08-02 R6] 역문제
  **버그 2건 발견·수정**: (a) GPL6246.annot 의 'Gene symbol' 은 3번째 열인데 2번째를 읽어
  매칭이 전멸하고 대체값으로 넘어가 있었다 (b) GSE54650 은 선형 강도값인데 log2 변환을
  빠뜨려 진폭이 344 로 나왔다. 둘 다 고친 뒤 아틀라스 실측 진폭 log2 중앙값 1.069.
  결과: 비행 대비 잡음 바닥 0.088 을 넘으려면 dphi >= 0.55h.
        웻랩 검출 하한(약 1h)이 그 위에 있다 -> **범위가 겹친다. 고리가 닫힌다.**
        실측 0.215 는 위상 이동만으로 설명하면 dphi ~ 1.30h 에 해당.
  판정: **완료**

[2026-08-02 R7-R9] 그림 4장 + 계획서 개정 + 검증
  그림 A(감사) B(정합성) C(마스킹) D(설계 사양).
  그림 B 는 초판 패널 제목이 과했다("부호가 반대다" / "전부 지연 방향").
  CI 를 확인하니 초파리 구간이 0 을 걸치고 체온 위상은 5 중 3만 0 을 배제했다. 제목·캡션 수정.
  산출: docs/계획서_드라이랩_v3.html (§2 나 / §3 전면 재작성),
        docs/제안_타절_v3.md (§1·§4 수정 제안 + 근거, 팀원 인계용)
  src/v3_verify.py 로 문서 수치 **58건 전건 대조, 실패 0**.
  판정: **완료**

[2026-08-02 R8b] 예상결과에 표·그림 추가 + 웻랩 예상결과 통합 (사용자 지적)
  지적: 예상결과와 사전분석 결과에 표도 그래프도 없고 판단만 들어가 있다.
        양식이 "시뮬레이션 자료 등 중간 결과물을 포함" 하라고 명시하는데 어겼다. 맞는 지적.

  추가한 것
    그림 E (신규) — 웻랩 예상 결과 시뮬레이션.
      세 T-cycle 군의 일별 acrophase 궤적을 예측하되, 오차막대를 임의로 그리지 않고
      R5 에서 실측한 심부체온 위상 오차(HLU전 0.31 / 중 2.77 / 해제후 1.67 h)를 넣었다.
      초판은 누적 위상으로 그려 패널마다 y 범위가 2~60h 로 달라졌고, 그 결과
      판정 간격 화살표 길이가 실제 크기와 반대로 읽혔다. 관측값(0-24h)으로 바꿔 축을 통일.
    표 1~4 신설 — 전수 스윕 규모 / 축별 정합성 / 필요 마리수 / 군별 예상 판정 간격

  Arnold tongue 에서 새로 나온 것
    세 T-cycle 의 난이도가 대칭이 아니다. 동조에 필요한 자극 세기가
      T=24h 0.00 / T=20h 0.05 / T=28h 0.13  -> T=28 이 T=20 보다 2.6배 강한 자극 요구.
    따라서 "세 군 모두 동조" 는 낙관 시나리오이고, 14시간 군의 실패는 가설 기각이 아니다.
    또 10시간 군(T=20)이 결정적 증거임을 명시 (내인성 23.7h 와 우연 일치 불가).

  체온 지표 근거 보강 (사용자 질문 "심부체온이 생체주기랑 무슨 관련인가")
    같은 동물·기간·임플란트에서 HLU 중 리듬이 유지된 개체:
      심부체온 5/6, 활동량 1/6, 피하온도 1/5
    -> 체온 리듬이 활동량의 부산물이 아니며, 피부가 아닌 '심부' 여야 함을 같은 자료가 보인다.
    다만 심부체온은 복강 텔레메트리 수술이 필요해 비침습 카메라 대비 비용·침습이 늘어난다.
    이 절충은 OPEN_QUESTIONS 로 남기고 웻랩 팀 판단에 맡긴다.

  txt 판을 HTML 에서 자동 생성하도록 변경 (src/v3_html_to_text.py).
    같은 내용을 두 벌로 손유지하면 반드시 어긋나므로 HTML 을 원본으로 고정.

  검증기가 실제로 오류 1건을 잡았다: 표 4 의 '표준오차 대비 6.5배' 는 6.4배가 맞다.
  v3_verify.py 108건 전건 통과.

[2026-08-02 R8c] 그림을 계획서 본문에 삽입 + 캡션을 본문 텍스트로 분리 (사용자 지적 2건)
  지적 1: 중간결과물을 따로 제출하는 식으로 썼는데 그게 아니다. 계획서 본문에 그림도
          같이 넣고 캡션을 달아야 한다. -> 맞다. (6) 항목을 '산출물 목록'에서
          '본문의 표 1~4, 그림 1~5 외에 추가로 산출되는 것'으로 재작성.
  지적 2: 이미지 안에 캡션이 박혀 있으면 한글에서 그림 크기를 조절할 때 글씨도 같이
          확대·축소돼 본문 글꼴과 어긋난다. -> 맞다. 그림 5장 전부 캡션을 빼고 재생성했다.
          캡션 원문은 results/v3/figures/captions.txt, caption_fig5.txt 로 내보내고
          문서 본문에 텍스트 문단으로 넣었다.

  그림 파일명을 figA~figE 에서 그림1~그림5 로 바꿔 문서 번호와 일치시켰다.

  배포 방식
    원본 HTML 은 그림을 상대경로로 참조하므로 구글 독스에 붙여넣으면 그림이 안 따라온다.
    src/v3_build_doc.py 가 PNG 를 base64 로 박아 넣은 docs/한글양식_최종_이미지포함.html
    을 생성한다(1.3MB). 브라우저로 열어 전체 선택·복사하면 그림째로 붙여넣어진다.
    이 배포본은 재생성 가능하므로 .gitignore 에 넣었다.

  분량 대책
    5쪽 제한에 그림 5장은 많다. 캡션에 우선순위를 표시했다.
    그림 5(예상결과)는 양식이 요구하는 시뮬레이션 자료라 필수.
    그림 1·3 은 표 1·표 3 과 내용이 겹치므로 먼저 지울 후보로 표시.

  v3_verify.py 에 그림 첨부 상태 검사 추가 -> 129건 전건 통과.

[2026-08-02 R8d] 연구방법에 5·6단계 신설 + 진동자 모델 정량 과장 철회 (사용자 지적)
  지적: "이렇게 어떻게 계산하고 구했는지가 연구방법에 없으니 헷갈린다."
  확인: 맞다. §3 에 그림 5(예상 결과)가 나오는데 그 산출 방법이 §2 에 없었다.
        더 큰 누락은 **분자시계 진동자 모델 자체가 연구방법에 아예 없었다**는 것.
        Arnold tongue 결과를 §3 에서 쓰면서 방법 절에 대응물이 없었다.

  조치 1 — §2 연구방법을 3개 묶음으로 재편하고 2단계를 신설
    [가] 자료 확보와 신뢰성 검사  1~3단계   -> 예상결과 (1)~(3)
    [나] 웻랩 설계값과 예상 결과   4~6단계   -> 예상결과 (3),(4)
    [다] 우주 자료와의 연결        7단계     -> 예상결과 (5)
    신설 5단계: 분자시계 진동자 모델(3변수 음성 되먹임, 651조합 수치적분,
                1:1 위상 잠금 판정 기준 4가지)
    신설 6단계: 예상 결과 시뮬레이션(동조/마스킹 일별 위상 계산 + 실측 잡음 부여,
                판정 간격이 표준오차 3배 초과면 검출 가능)
    각 묶음에 대응하는 예상결과 번호를 표기해 방법->결과 추적이 되게 했다.

  조치 2 — 철회: "T=28h 가 T=20h 보다 2.6배 강한 자극을 요구한다"
    oscillator_model.py 의 docstring 에 "정량적 예측이 아니라 정성적 구조 예측에만
    쓴다" 고 이미 적혀 있었다. Hill 계수 등 파라미터가 실측이 아니고 자극 세기도
    모델 단위이며 격자 간격이 0.01 이라 배수를 정량 지표로 쓸 수 없다.
    -> "뚜렷하게 강한 자극을 요구한다(모델 단위 0.13 대 0.05)" 로 교체하고,
       모델의 사용 범위 한계를 방법(5단계)과 캡션 양쪽에 명시.
       방향(28h 가 더 어렵다)은 유지. 순서는 신뢰할 수 있다.

  그림 캡션 원문(스크립트 내)도 같은 문구로 동기화. v3_verify.py 137건 전건 통과.

[2026-08-02 R5b] 검정력을 계획서 판정 기준(회귀선)으로 재계산 — 사용자 지적, 결과 변경
  지적: "본 연구의 판정 기준(회귀선 절편 차이)을 그대로 시뮬레이션한다고 썼는데,
        네 설명대로면 p<0.05 인 횟수를 세는 것 아니냐. 이건 뭘 시뮬레이션한 거냐."
  확인: 지적이 맞다. R5 는 각 구간의 **평균 위상**을 비교했고, 계획서 기준은
        **회귀선의 기울기와 절편** 비교다. 둘은 같지 않다.
          - 평균은 구간 내 위상 표류가 없다고 가정하는데, T != 24h 군은 하루
            (T-24)시간씩 체계적으로 표류한다. 표류하는 계열의 산술평균은 위상이 아니다.
          - 회귀는 기울기를 함께 추정하므로 절편 표준오차가 커진다.
            처치 시작일 기준으로 0.762h -> 1.482h (1.95배).

  조치: src/v3_r5b_power_regression.py 신설. 검정을 계획서 문구대로 둘로 나눴다.
        검정 A(절편) = 새 위상 형성 / 검정 B(기울기) = 주기 변화.
        통계량이 정규분포의 선형결합이므로 비중심 t 로 닫힌 형태 계산.
        몬테카를로로 검산했고, 검산 코드의 생성 모형 오류(처치군에 baseline 선의
        기준일 오프셋 (tau-24)*d0 를 빼먹어 효과를 2.0 이 아닌 2.9 로 생성)를
        자체 검산이 잡아냈다. 수정 후 0.754 대 0.752 로 일치.

  결과 변경 (표 3 교체)
    절편 검정 필요 마리수 (심부체온): dphi=1h 20 / 2h 8 / 3h 6
      -> 기존 표(8/4/4)는 낙관적이었다. 2배 늘었다.
    기울기 검정 필요 마리수 (심부체온): T=24h 10 / T=20h 4 / T=28h 6

  새로 드러난 설계 사실 2건
    (1) 두 검정의 난이도가 실험군마다 뒤집힌다. 10·14시간 군은 하루 4시간씩
        표류하므로 기울기 검정이 4~6마리로 강력하고, 12시간 군은 기울기 차가
        0.3h/일뿐이라 절편 검정에 의존해 가장 많은 마리수를 요구한다.
        -> 군별 주 검정을 사전 지정하도록 계획서에 명시.
    (2) baseline 3일이면 그 회귀선의 기울기 표준오차가 0.219 h/일 로
        참 표류(0.3 h/일)와 구별되지 않는다. 절편 검정이 이 선의 연장에
        의존하므로 baseline 을 5~7일로 늘리는 것이 동물 수를 늘리는 대안이다.

  그림 4(A) 를 회귀 기준 검정력으로 재생성. 그림 5 의 8마리 근거를
  'dphi=1h 검출' 에서 '절편 검정으로 dphi=2h 검출' 로 정정.
  v3_verify.py 145건 전건 통과.

[2026-08-02 R5b-2] baseline 연장 권고 철회 + 격자 세분화
  직전 턴에 "처치 전 구간을 5~7일로 늘리면 (절편) 검정력이 개선된다" 고 썼다.
  계산해 보니 틀렸다.
    절편 검정 합성 SE : baseline 3일 1.482h -> 7일 1.429h -> 14일 1.416h
    필요 마리수는 전 구간 7마리로 동일. 개선 없음.
    이유: SE 의 대부분이 처치 구간 위상 오차(2.77h)에서 온다. baseline 쪽 기여는
          0.474h 뿐이라 0 으로 만들어도 합성값이 거의 안 변한다.
    기준일 d0 를 바꾸는 것도 최대 4% 개선이라 무의미(최적 d0=4.5일, SE 1.418h).
    -> 절편 검정은 마스킹이 지배한다. 설계로 못 바꾼다.

  대신 baseline 연장이 실제로 효과가 있는 곳을 찾았다: **기울기 검정**.
    기울기 차 SE : 3일 0.286 -> 5일 0.208 -> 7일 0.193 h/일 (5일에서 포화)
    12시간 군(T=24h) 필요 마리수 : 10 -> 6 (3일 -> 5일)
    12시간 군은 기울기 차가 0.3h/일 뿐이라 세 군 중 유일하게 절편 검정에 의존한다.
    -> "처치 전 5일" 은 동물 수를 늘리지 않고 그 군을 보강하는 방법이다.
    계획서 문구를 이 내용으로 교체.

  격자 세분화: n 을 (4,6,8,...) 에서 (3,4,5,6,7,8,10,...) 으로,
  처치 전 구간을 (3,5,7) 로 넣어 재산출. 표 3 수치가 바뀌었다.
    심부체온 절편 dphi=2h : 8 -> 7 (이전 값은 격자가 성겨서 올려 잡힌 것)
    심부체온 기울기 T=20h/28h : 4/6 -> 3/3
  그림 5 의 8마리는 7마리에 여유를 둔 값으로 문구 정정.
  v3_verify.py 145건 전건 통과.

[2026-08-02 R8e] 4단계에 도출 논리 보강 (사용자 요청: "검정으로 구하는 방법을 잘 작성")
  배치 판단: 도출 논리는 방법(§2 4단계), 결과 수치는 예상결과(§3-(3)).
    심사 배점의 "연구 방법의 적절성 및 차별성"(10점)과
    "연구 추진전략이 체계적으로 수립되었는가"(25점 항목 내)가 방법 쪽에 걸린다.

  4단계에 새로 넣은 것 2가지 (설명 중 누락이 드러난 부분)
    (1) 기울기가 뜻하는 것 — 주기가 T 이면 24시간 시계에서 정점이 하루
        (T-24)시간씩 이동한다. 조수가 24시간 50분 주기라 만조가 매일 50분씩
        늦어지는 것과 같은 원리로 설명. 이게 없으면 '하루 4시간 표류' 가
        어디서 나온 수치인지 독자가 알 수 없다.
    (2) 구간 평균이 아니라 회귀를 쓰는 이유 — T != 24h 군은 구간 내 위상이
        표류하므로 산술평균이 위상이 아니다. 대신 절편 표준오차가 1.95배 커지고
        그 손해를 반영한 값을 쓴다.

  예상결과 (3) 은 표류량(4h/일)과 측정 오차(2.77h)를 나란히 놓아
    '신호가 잡음보다 크다' 는 비교가 보이게 문장 교체.

  4단계 분량 4줄 -> 7줄. v3_verify.py 151건 전건 통과.

[2026-08-02 R8f] 5단계에 모델 수식·수치해법·격자 근거 전면 기술 (사용자 지적 2건)
  지적 1: "651조합은 뭘 근거로 한 거냐"
    확인 결과 정직하게 나눠 답해야 한다.
      범위는 근거 있음 — 주기 19.6~28.3h 는 실험의 20/24/28h 를 덮도록,
        세기 상한 0.20 은 그 위에서 고차(n:m) 잠금이 섞여 1:1 판정이 무의미해져서.
        (실측 확인: 세기 0.18 이상 93조합 중 진폭 사멸 0건, 회전수 불일치 49건,
         위상 표류 과다 39건 -> 실패 원인은 진폭 사멸이 아니라 고차 잠금이었다.
         앞선 설명에서 이를 진폭 사멸로 말했던 것은 부정확했다.)
      해상도 31x21 은 근거 없음 — 계산 편의다. 문서에 그대로 명시했다.
      부수 확인: 격자점이 20/24/28h 에 각각 0.074/0.051/0.028h 이내로 근접(운).
                T=28h 가 격자 상한 28.26h 에서 0.26h 안쪽이라 그보다 긴 주기는 미탐색.

  지적 2: "미분방정식 모델의 수식 정도는 계획서에 있어야 하지 않나"
    맞다. 모델을 썼다고만 쓰고 수식이 없으면 검증이 불가능하다.
    5단계를 4줄 -> 13줄로 확장하며 넣은 것:
      · 3변수 ODE 수식 전체 (pre 블록)
      · 파라미터 11개 값과 Hill n=8 의 의미·한계
      · 중력의 두 입력 경로(전정계 u(t) / 미토파지 kdeg)와 각각의 문헌 근거
      · 자극 형태의 단순화 명시 — 모델은 주기당 1시간 펄스인데 실제 HLU 는
        주기의 절반 지속. 그래서 필요 세기를 실험 조건으로 환산할 수 없다.
        (이 불일치는 이전까지 어디에도 적혀 있지 않았다)
      · 수치해법: RK4, dt=0.02h, 버닝 600h + 과도 150tau, 관측창 60tau
      · 위상을 B 가 아니라 P 로 재는 이유
      · 동조 판정 4조건의 수치
      · 한계

  txt 변환기가 <pre> 블록의 줄바꿈을 살리도록 보강.
  v3_verify.py 167건 전건 통과.

[2026-08-02 R9b] 분량 축소 + 한글 제출본 생성 (사용자 지시 3건)
  지시 1: 5단계(진동자 모델) 삭제 — 분량 초과
    민감도 검사 중 파라미터 주입 버그를 발견해 재실행 중이었으나, 그와 별개로
    삭제 결정. 삭제 근거는 이미 충분했다:
      파라미터가 실측 아님 / 선택 과정 미기록 / 자극 파형이 실험과 불일치
      (주기당 1시간 펄스 대 절반 지속) / 세기가 물리 단위 아님 /
      Arnold tongue 은 급성 경로만 봄 / kdeg -20% 에서 비대칭이 사라짐(비 1.00)
    삭제 대상: 방법 5단계, 그림의 Arnold tongue 패널, 예상결과 (4)의 난이도 문단.
    코드(scripts/oscillator_model.py, src/v3_r5c_*)는 남긴다. 발표 질의 대비용.

  지시 2: 7단계 결과를 활용방안으로 이관 제안 -> 예상결과 (5) 삭제
    방법 6단계(역문제 설정)는 남기고 결과만 뺐다. 묶음 라벨을
    '-> 활용방안 절로 이관 제안' 으로 바꿔 담당자에게 넘어가게 표시.
    주의: 방법에 6단계가 있는데 대응 결과가 §3 에 없다. 활용방안 담당자가
          반드시 받아야 하며, 안 받으면 방법 6단계도 빼야 한다.

  지시 3: 예상결과 (2)(축별 정합성) 삭제
    표 2·그림 2(축별 정합성)와 함께 제거. 다만 1차 지표를 심부체온으로 두는
    근거가 사라지므로, '심부체온 5/6 대 활동량 1/6 대 피하온도 1/5' 한 줄을
    (2)[구 (3)]로 옮겨 살렸다.

  재구성 결과
    방법 7단계 -> 6단계 / 예상결과 6항목 -> 4항목
    그림 5장 -> 3장 (데이터감사 / 마스킹+검정력 합본 / 웻랩예상결과)
    표 4개 -> 3개

  한글 제출본
    src/v3_make_hwp_docx.py 신설. HWP 5.0 은 폐쇄 포맷이라 직접 생성이 어려우므로
    .docx 로 만든다. 한글 2010+ 는 .docx 를 열고 '한글 문서로 저장' 하면 된다.
    양식 서식 적용: □ = HY헤드라인M 14pt, ◦/-/※ = 휴먼명조 14pt 줄간격 160%,
    캡션 12pt, A4 여백, 표는 Table Grid, 그림 폭 15.5cm.
    eastAsia 글꼴 속성까지 지정해야 한글 글꼴이 실제로 적용된다.
    제출본에서 작업 메모([축약 가능] 등)를 자동으로 걸러낸다.
    -> docs/KASA_연구계획서_2연구방법_3예상결과.docx

  v3_verify.py 에 docx 점검 추가(표·그림 개수, 작업 메모 잔류). 129건 전건 통과.

[2026-08-02 R9c] 진짜 .hwp 생성 (사용자 지시: "한글파일로 만들라고")
  .docx 로 대체했던 것을 정정. 이 PC 에 한글 13.0 이 설치돼 있고
  HWPFrame.HwpObject COM 이 등록돼 있어 한글 자신에게 변환을 시켰다.

  막혔던 지점 두 개
    (1) 첫 실행이 출력 없이 멈춘 것처럼 보였다 -> stdout 버퍼링이었다.
        python -u 로 확인하니 COM 자체는 정상(Version 13.0.0.1053).
    (2) hwp.Open(path, "MSWord", ...) 가 False 를 반환했다.
        포맷 문자열을 주면 실패하고, 인자 없이 hwp.Open(path) 로 부르면
        확장자 자동 판별로 정상 동작한다. 이 버전에서 "MSWord" 는 인식되지 않는다.

  검증
    OLE 복합문서 서명 확인, 한글로 다시 열어 13쪽 확인.
    GetTextFile("TEXT") 가 '웻랩' 을 'ㅤㅇㅞㅅ랩' 으로, em dash 를 '&#8212;' 로
    내보내 문서가 깨진 줄 알았으나, PDF 로 뽑아 확인하니 '웻랩' 12회 온전하고
    자모 분해 0회. **GetTextFile 추출 방식의 문제였고 문서는 정상이다.**

  산출: docs/KASA_연구계획서_2연구방법_3예상결과.hwp (672KB, 13쪽)
        src/v3_make_hwp.py

  ** 미해결 — 분량 **
    13쪽이다. 양식은 §1~§5 전체를 5쪽 안에 넣으라고 한다.
    14pt·줄간격 160% 에서 쪽당 약 730자이므로 5쪽 전체가 약 3,600자인데
    드라이랩 담당분만 9,535자다. 웻랩·§1·§4·참고문헌을 빼면 드라이랩 몫은
    현실적으로 1.5~2쪽(약 1,100~1,500자)이다. 지금의 1/6 로 줄여야 한다.
    다듬기가 아니라 재작성이 필요하다. 목표 분량을 정한 뒤 진행할 것.
