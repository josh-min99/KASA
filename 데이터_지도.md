# 데이터 지도

> **주제와 무관하게 재사용 가능한 문서.** 어떤 데이터가 어디에 있고, 어떻게 접근하며,
> 무엇이 들어 있는지를 기록한다. 연구 주제가 바뀌어도 이 문서의 §1–§3은 그대로 쓸 수 있다.
>
> 재현: `python scripts/build_catalog.py` → `python scripts/build_geo_index.py`

---

## 1. NASA OSDR 접근 방법

### 1.1 엔드포인트 (2026-07 동작 확인)

| 용도 | 엔드포인트 | 상태 |
|---|---|---|
| 스터디 검색 | `osdr.nasa.gov/osdr/data/search` | 정상. `size`/`from` 페이징 필요 |
| 파일 목록 | `osdr.nasa.gov/osdr/data/osd/files/{id}` | 정상. ISA 경로 획득용 |
| 메타데이터 | `osdr.nasa.gov/osdr/data/osd/meta/{id}` | ⚠️ **factor value 를 null 로 반환** |
| **ISA 아카이브** | files API 응답의 `*-ISA.zip` | ✅ **메타데이터의 유일한 정본** |
| 페이로드 목록 | `osdr.nasa.gov/geode-py/ws/api/payloads` | 정상. 334건 |
| 페이로드 상세 | `osdr.nasa.gov/geode-py/ws/api/payload/{name}` | 정상. `parents.GLDS_Study` 에 연결 스터디 |
| GLOpenAPI | `visualization.genelab.nasa.gov/GLOpenAPI/` | ❌ 404 (서비스 중단) |
| RadLab | `visualization.osdr.nasa.gov/radlab/api` | ❌ 500 |

인증 불필요. 전부 무료.

### 1.2 반드시 지킬 것

**① 메타데이터는 ISA.zip 에서 읽는다.**
`meta/{id}` JSON API 는 `Factor Value[...]` 를 null 로 반환한다. 실제 값
(`0.33G by centrifugation` 등)은 ISA 아카이브 안의 `s_*.txt` 에만 있다.

```python
files = GET /osdr/data/osd/files/{id} → study_files
isa   = [f for f in files if f["file_name"].endswith("ISA.zip")]
zip   = GET "https://osdr.nasa.gov" + isa[0]["remote_url"]
# zip 안의 s_*.txt(샘플), a_*.txt(어세이) 를 탭 구분으로 파싱
```

**② 검색 인덱스를 신뢰하지 않는다.**
Search API 는 ISA 의 모든 컬럼을 노출하지 않는다. "어떤 필드가 존재하는가"를 확인하려면
ISA 를 직접 열어야 한다. 검색 인덱스만 훑으면 조용히 누락된다.

**③ organism 필터로 좁히지 않는다.**
`fvalue=Mus musculus` 로만 검색하면 쥐(Rattus) 스터디가 통째로 빠진다.
설치류 연구에는 최소 `Mus musculus` + `Rattus norvegicus` 둘 다 필요하다.

**④ 스터디 집합은 payload API 로 교차검증한다.**
검색 인덱스 기반 추출보다 payload 의 `parents.GLDS_Study` 가 완전하다.
예: Hindlimb Unloading 은 검색 기반 29건 vs payload 43건.

**⑤ 전수가 필요하면 ID 를 순회한다.**
OSD ID 는 1 부터 약 1,005 까지 존재한다(1010 이상은 없음).
`scripts/build_catalog.py` 가 이 방식으로 필터 없이 전수 카탈로그를 만든다.

### 1.3 처리된 발현 데이터

FASTQ 재정렬은 불필요하다. GeneLab 표준 워크플로 산출물이 파일 목록에 있다.

```
GLDS-###_rna_seq_Normalized_Counts_GLbulkRNAseq.csv        정규화 counts
GLDS-###_rna_seq_VST_Counts_GLbulkRNAseq.csv               분산안정화
GLDS-###_rna_seq_differential_expression_GLbulkRNAseq.csv  DE 결과 + ENSEMBL/SYMBOL 매핑
```

원시 FASTQ 까지 있어 스터디당 수 GB 다. **processed counts 만 받을 것.**

---

## 2. 전수 카탈로그

`scripts/build_catalog.py` 가 OSD-1 ~ OSD-1100 을 순회하며 존재하는 모든 스터디의
ISA 를 열어 **모든 컬럼과 값 샘플**을 기록한다. 필터링은 하지 않는다.

| 산출 | 내용 |
|---|---|
| `data/catalog_studies.csv` | 스터디 1행 — organism, material, n_samples, assays, factors, counts 보유 여부 |
| `data/catalog_columns.csv` | 스터디 × 컬럼 1행 — 컬럼명, 고유값 수, 값 샘플 |
| `cache/isa/` | 내려받은 ISA 아카이브 (재실행 시 재사용) |

**필터를 나중에 거는 것이 핵심이다.** 초기 분석의 누락은 전부 "무엇을 찾을지 미리 정하고
검색한" 데서 나왔다. 카탈로그를 먼저 만들면 그 실수가 구조적으로 불가능해진다.

선별은 `scripts/select_datasets.py` 가 담당하며, 기준을 코드에 명시했다.

---

## 3. GEO 참조 색인

`scripts/build_geo_index.py` → `data/geo_index.csv` (222건)

OSDR 은 우주생명과학 데이터를 모으지만, **지상 circadian 기준 데이터는 GEO 에 있다.**

| 검색 태그 | 용도 |
|---|---|
| `SCN_circadian` | 시교차상핵 circadian 시계열 |
| `circadian_atlas` | 조직별 circadian atlas |
| `spaceflight_rodent` | 설치류 우주비행 |
| `hypergravity_rodent` | 설치류 과중력 (대부분 오탐 — 프로토콜상 원심분리에 매칭) |

### 주요 데이터셋

| 접근번호 | n | 내용 | 용도 |
|---|---|---|---|
| **GSE54650** | 288 | 마우스 12조직 × 24시점 circadian atlas (microarray) | 위상 예측기 학습, 진폭 추정 |
| **GSE70391** | 8 | **SCN** RNA-seq, CT22–CT64 (SuperSeries GSE70392) | SCN 특이 리듬 기준 |
| GSE150770 | 192 | SCN VIP 뉴런과 리듬 | SCN 분산 추정 |
| GSE118404 | 96 | SCN 시공간 단일세포 | SCN 세포 이질성 |
| GSE196430 | 54 | 중추시계가 대사 리듬을 구동 | 중추–말초 연결 |
| GSE162309 | 48 | **마우스 시상하부** RNA-seq, 무중력·소음·저기압 | SCN 포함 조직, 모사 조건 |
| GSE222998 | 84 | 중력·기계력과 미토콘드리아 번역 | 미토파지 축과 접점 |
| GSE12132 | 72 | 쥐, 3가지 중력 조건 (유선·간·지방) | 구형 array, 조직 부적합 |
| GSE239336 | 48 | 우주비행 마우스 뇌, 항산화제로 완화 | 인공중력 아님 |

> ⚠️ `GSE54650` 은 GC-RMA **선형 강도**다(중앙값 100, 최대 45,308). log2 변환 후 사용할 것.

---

## 4. 데이터 확정 (전수 카탈로그 기반)

`scripts/select_datasets.py` 산출 → `data/dataset_selection.csv`
카탈로그 626건 · 컬럼 레코드 33,368건에서 선별.

| 등급 | 기준 | 건수 |
|---|---|---|
| **1 용량반응** | 설치류 + 중력 3단계 이상 + 발현행렬 | **4** |
| **2 위상통제** | 설치류 + 샘플 간 시각 변이 + 발현행렬 | **1** |
| 3 시계열 | 설치류 + 생리·행동 판독 | 12 |
| 4 이진중력 | 설치류 + 중력 2단계 + 발현행렬 | 83 |
| 5 표현형전용 | 설치류 + 중력 3단계, 발현행렬 없음 | 4 |

### 4.1 등급 1 — 중력 용량반응 검정 가능 (4건)

| OSD | 종·조직 | 중력 단계 | n | 플랫폼 |
|---|---|---|---|---|
| **63** | 쥐 · 간 / 복부지방 / 유선 | 0 / 1 / **2 g** | 72 | Affymetrix array |
| **714** | 마우스 · 가자미근 | 0 / **1/6** / 1 g | 21 | RNA-seq |
| **758** | 마우스 · 우측 망막 | 0 / 0.33 / 0.66 / 1 g | 35 | RNA-seq |
| **759** | 마우스 · 우측 시신경 | 0 / 0.33 / 0.66 / 1 g | 35 | RNA-seq |

**OSD-63 이 가장 중요하다.** 유일한 **과중력(2 g) 설치류 전사체**이며, 원심분리로
지상에서 수행돼 착륙 교란이 없다. `Factor Value[Altered Gravity]` =
`1G on Earth / 2G by centrifugation / uG`, 임신·수유 2조건, 2G 군은 조직·조건당 n=5.
STS-70 비행 시료(유선, uG, n=4)도 포함한다.

> 관련 문헌: *Hypergravity disruption of homeorhetic adaptations to lactation in rat dams
> include changes in circadian clocks*, Biology Open (2012).
> **과중력이 생체시계를 교란한다는 보고가 이미 이 데이터에 붙어 있다.**

OSD-714 의 1/6 g 는 달 중력, OSD-758/759 는 4단계 용량반응(MHU-8, 44/63/77 rpm).

### 4.2 등급 2 — 위상 통제 가능 (1건)

**OSD-612** 만 해당한다. 상세는 §5.2.

### 4.3 등급 5 — 중력 3단계이나 전사체 없음 (4건)

| OSD | 측정 | 중력 단계 | n |
|---|---|---|---|
| 616 | hindlimb (force plate) | 0 / 0.4 g / 1 g | 239행 |
| 617 | 발정주기 (질 도말) | 0 / 0.4 g / 1 g | 300행 |
| 652 | 악력 (force transducer) | 0 / 0.4 g / 1 g | 595행 |
| 653 | 경골 (pQCT) | 0 / 0.4 g / 1 g | 238행 |

쥐 120마리, 28일 종단(T0/T07/T14/T21/T28), 12:12 LD, 채취 08:00–10:00.

> ⚠️ **중력 감지 경로 연구에는 쓸 수 없다.** 여기서 0.4 g 는 "partial weight bearing",
> 즉 현수 장치로 체중을 덜어내는 방식이다. 근골격계 부하는 줄지만
> **이석기관은 여전히 1 g 를 감지한다.** 근골격계 연구에는 타당한 부분중력 모사지만,
> 전정계 매개 가설에는 부분중력 모델이 아니다.
> 원심분리(OSD-63/714/758/759)와 이 점에서 근본적으로 다르다.

---

## 5. 시간축 현황

### 5.1 결론

리듬 분석에 필요한 것은 시각의 **기록 여부**가 아니라 **샘플 간 변이**다.
설치류 281건 ISA 전수를 값 수준으로 검사한 결과:

| 유형 | 건수 | 예 |
|---|---|---|
| 명암주기 기준점만 기록 (`lights on at 7:00 GMT`) | 13 | OSD-463, 464, 506, 512, 513, 515, 525, 576, 665, 666, 667, 770 |
| 채취 시각대를 스터디 상수로 기록 | 1 | OSD-617 (`Between 8:00 and 10:00 AM`) |
| **샘플별 시각 + 변이 있음** | **1** | **OSD-612** |
| 궤도 영상 녹화 시각 | 1 | OSD-952 (`Recording Time (UTC)`, 65개 값) |

`Factor Value[Time]` 은 대부분 **경과일**이다(T0/T07/T14/T28, 또는 1/2/4/7일). 시각이 아니다.

### 5.2 OSD-612 상세

유일하게 샘플별 희생 시각이 기록되고 변이가 있는 스터디다.

| | |
|---|---|
| 미션·조직 | RR-10, 마우스 좌측 대뇌반구 |
| 어세이 | RNA-seq + single-cell ATAC-seq (GeneLab counts 있음) |
| 설계 | 우주비행 5 / 지상대조 5 |
| ZT 기준 | `12 h light/dark cycle, lights on at 7:00 GMT` |
| 희생 시각 | 비행 09:34–14:09 GMT / 지상 04:34–09:10 EST |
| **환산 ZT** | 2.57 / 3.58 / 4.30 / 6.43 / 7.15 — **지상대조가 시각 매칭돼 있음** |

**한계**: ZT 폭 4.6시간, 군당 n=5, 인공중력군 없음.
시계 유전자가 ZT 를 따라가는지 검정하면 두 군 간 부호 일치 8/16(우연 수준),
상관계수 간 상관 0.154 로 **자기검증되지 않는다.**

### 5.3 OSD-952 — 궤도 행동 시계열

RR-1 미션 ISS Rodent Habitat 영상을 SLEAP + DeepEthogram 으로 프레임 단위 분류.

- 고유 프레임 1,175,486 / 비행 Day 0–32 / 8개 행동 범주
- `Light Cycle Phase` (Light/Dark) 라벨, `Recording Time (UTC)` 65개 값
- 미세중력에서 야행성 리듬 유지 확인 (탐지 편향 통제 후에도 유지)
- **인공중력군 없음**, 녹화 시각 불균일

---

## 6. 방법론 정정 기록

초기 분석에서 범위 설정을 세 번 잘못 잡았다. 원인은 모두 동일하다 —
**무엇을 찾을지 미리 정하고 검색했다.**

### 6.1 탐색 범위 오류 (3건)

| # | 오류 | 결과 | 수정 |
|---|---|---|---|
| 1 | `organism = Mus musculus` 로만 검색 | 쥐 스터디 전부 누락 (OSD-63, 616/617/652/653) | Rattus 포함 |
| 2 | 검색 API 인덱스 필드만 정규식 검사 | ISA 전용 컬럼 누락 (OSD-612 의 희생 시각) | ISA 전수 개봉 |
| 3 | `Altered Gravity` factor 보유만 대상 | 다른 표기 누락 | 필터 없는 전수 카탈로그 |

**뒤집힌 결론 2건**

- ❌ "희생 시각 기록 0건" → 정확히는 **"샘플 간 시각이 달라지는 스터디는 1건(OSD-612)"**.
  그마저 ZT 폭 4.6 h · n=5 로 리듬 분석에는 부족하다.
- ❌ "설치류 과중력 전사체 없음" → **OSD-63 이 존재한다.** 쥐 2 g 원심분리, 3조직, 마이크로어레이.
  `Simulated_Hypergravity` payload 에 없어서 놓쳤다 — 그 payload 는 비설치류 전용이고,
  OSD-63 은 STS-70 비행 프로그램에 묶여 있다. **payload 로도 완전하지 않다.**

### 6.2 선별 코드 자체의 버그 (4건)

전수 카탈로그를 만든 뒤에도 선별 코드에서 오류가 나왔다. 전부 조용히 틀린다.

| # | 버그 | 증상 |
|---|---|---|
| 1 | 오탐 제거 정규식 `1\s*:\s*\d{2,}` 에 `\b` 누락 | 희석비 1:100 을 거르려다 **시각 11:18 의 '1:18'** 에 매칭 → OSD-612 탈락 |
| 2 | 값들을 이어붙인 문자열로 검사 | 한 값의 오탐이 나머지 전체를 오염 |
| 3 | `mars` 를 화성으로 해석 | JAXA 장비명 **MARS**(Multiple Artificial-gravity Research System)가 0.38 g 로 오인 → OSD-714 에 없는 단계 추가 |
| 4 | 발현행렬을 RNA-seq counts 로만 판정 | 마이크로어레이 스터디 10건 누락, **OSD-63 이 '표현형 전용' 으로 오분류** |

교훈: **정규식 오탐 필터가 정탐까지 지운다.** 필터를 추가할 때마다 그것이
무엇을 지우는지 역으로 확인해야 한다.

### 데이터 취급 함정

| # | 함정 | 증상 |
|---|---|---|
| 1 | MGI 심볼 변경 `Arntl` → `Bmal1` | 예외 없이 NaN |
| 2 | GSE54650 이 선형 강도인데 log2 로 오인 | 진폭이 수백 단위 |
| 3 | `Factor Value[Time]` 을 시각으로 오인 | 실제로는 경과일 |
| 4 | 컬럼명만 보고 판단 | 값을 봐야 시각/경과일 구별 가능 |
| 5 | `centrifug` 정규식 | 프로토콜상 시료 원심분리에 대량 오탐 |

---

## 7. 파일

```
scripts/build_catalog.py       OSD 전수 순회 → 카탈로그
scripts/build_geo_index.py     GEO 참조 색인
scripts/select_datasets.py     카탈로그 → 선별 (기준 코드에 명시)
scripts/time_axis_audit.py     시간축 전수 감사

data/catalog_studies.csv       스터디 카탈로그 626건
data/catalog_columns.csv       컬럼 카탈로그 33,368건
data/dataset_selection.csv     ★ 최종 선별 결과 (등급 포함)
data/geo_index.csv             GEO 색인 222건
data/studies_gravity.csv       (구버전 — 마우스 한정. 참고용으로만 보존)
data/ground_studies.csv        (구버전 — 검색 기반, 과소집계. 참고용으로만 보존)
cache/isa/                     ISA 아카이브 캐시 626건
```

> `studies_gravity.csv` 와 `ground_studies.csv` 는 정정 전 방법으로 만든 것이다.
> **`dataset_selection.csv` 를 정본으로 쓸 것.**

## 8. 다음에 확인할 것

- **OSD-63 의 시계 유전자 반응** — 2 g 원심분리군 vs 1 g 대조. 우리 웻랩과 동일 패러다임이다.
  간·지방·유선은 말초시계이므로 SCN 은 여전히 공백이다.
- OSD-63 에 채취 시각이 없다. 임신·수유라는 강한 생리 상태가 교란변수다.
- 등급 4(이진중력) 83건 중 뇌 조직: OSD-202(brain), 525·561·563(소뇌),
  562·564(해마), 612·613(대뇌반구). **SCN 특이 조직은 카탈로그 626건 전체에 없다.**
